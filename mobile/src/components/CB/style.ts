// components/CB/style.ts
import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  loadingContainer: {
    padding: 10,
    alignItems: 'center',
    justifyContent: 'center',
    height: 40,
  },
});